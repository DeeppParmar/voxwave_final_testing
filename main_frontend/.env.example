# Example environment for VoxWave frontend
#
# Leave these empty for local dev (Vite proxy -> localhost:8000)
# Set them for ngrok/Vercel so the frontend talks to your public backend URL

VITE_API_URL=
VITE_WS_URL=

# Optional: used only by the Vite dev server proxy (npm run dev)
# Example: http://localhost:8000 or https://xxxx.ngrok-free.app
VITE_PROXY_TARGET=
