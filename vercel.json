{
  "version": 2,
  "builds": [
    {
      "src": "main_frontend/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/main_frontend/dist/$1"
    }
  ]
}