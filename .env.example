# Example environment for VoxWave backend
#
# Used to generate room invite links when the backend and frontend are on different domains.
# If unset, backend uses the request Origin header or its own base URL.
FRONTEND_BASE_URL=

# Backend port for local/hosted usage
PORT=8000
